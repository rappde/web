import { GeoPage } from '@/components/GeoPage'
import { geoPages } from '@/content/datamoshing'

/** /datamoshing-tools — comparison table page. */
export default function DatamoshingTools() {
  return <GeoPage page={geoPages.datamoshingTools} />
}
